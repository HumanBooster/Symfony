<h3>Liste des commentaires</h3>
<div class="well">
<ul>
{% for commentaire in commentaires %}
	<li>{{ commentaire.id }} - {{ commentaire.titre }} - <a href="{{ path("commentaire_edit", {'id':commentaire.id}) }}">Editer</a> - <a href="{{ path("commentaire_delete", {'id':commentaire.id}) }}">Supprimer</a></li>
{% else %}
	<li>Pas d'commentaire trouvé</li>
{% endfor %}
</ul>
<a href="{{ path("commentaire_add") }}">Ajouter un commentaire</a>
</div>